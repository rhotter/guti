# Legacy jwave ultrasound

This folder preserves the older JAX/jwave ultrasound experiments and helpers.
The active ultrasound modality uses `guti.modalities.us.modality` plus the
jwave-free free-field helper in `guti.modalities.us.utils`.
